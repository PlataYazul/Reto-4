# Reto-4!

```pseudocode
  instrucciones
  [variables]
Z : entero
e : entero
inicio
  Z := 2
  Mientras (Z < e) hacer
    Si modulo(Z,e) == 0 entonces
      escribir("Z es divisor de e")
    sino
      escribir("Z no es divisor de e")
    e := e + 1
  Fin mientras
fin
```

